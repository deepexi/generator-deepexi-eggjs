'use strict';

const assert = require('assert');
 const domain = require('../../modules/domain/index');

describe('modules/domain/test/index.test.js', () => {
    it('should pass', () => {
        assert(domain);
    })
})
